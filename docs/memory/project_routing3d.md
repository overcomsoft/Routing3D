---
name: project-routing3d
description: "Routing3D 프로젝트 개요 — 플랜트 배관 3D 직교 라우팅 엔진, Python 실험 → C++ 엔진화"
metadata: 
  node_type: memory
  type: project
  originSessionId: 12358f83-5328-4fc4-96ed-e4e7bd4740f5
---

Routing3D = 플랜트 배관 3D 직교 자동 라우팅 엔진. Python 알고리즘 실험(Phase 1) → 인터페이스 동결(Phase 2) → C++ 엔진화 + pybind11(Phase 3).

> **2026-05-30 — 프로젝트 컨텍스트가 repo 의 [CLAUDE.md](CLAUDE.md) 로 이동**(다른 PC/새 세션 즉시 재개용, 커밋 `712d83a`). 이 사용자 메모리는 개인 환경·시간순 작업 로그 보조용으로 유지 — 최신 프로젝트 상태가 필요하면 CLAUDE.md 가 정답.

- **신규 개발**. 기개발 C++ 프로토타입·C# 뷰어 없음. 인접 `..\SpaceAI\`(C# 직교 A* + 동일 DB)는 개념·데이터·**UI 스타일** 참조. **2026-05-30 확인: 읽기 가능**(routing_viewer_wpf/MainWindow.xaml 등). 직접 포팅은 안 함.
- **단위**: 모든 좌표·치수 **mm**. 기본 셀 50mm(`RouteParams.cell_mm`).
- **환경**: 루트 `.venv`. `python_experiments` 를 editable 설치(`pip install -e "python_experiments[viz]"`)하면 작업 디렉토리·PYTHONPATH 무관하게 `python -m routing3d_py.<module>` 실행. 테스트 `python -m pytest python_experiments`.

**Phase 1 현재 표준** (구현·테스트 완료, 마지막 확인 2026-05-28 / pytest 193 통과(+db 6 deselect) — 상세 현황은 코드·git 이 정답):

- `occupancy` — 추상 `OccupancyMap` + Dense/Sparse/BitPacked 백엔드(질의 인터페이스 동일, A* 가 백엔드 무관). `copy()` 지원. 옥트리/VDB 는 Phase 3.
- `obstacle_db` — `TB_BIM_OBSTACLES` 로더 → 점유맵. [[reference-obstacle-db]]
- `scene` — SpaceAI 프로젝트 단위 씬(장애물/메인장비/종단/PoC페어/유틸리티). [[reference-routing-scene]]
- `astar` — 직교 A*(기본) + `astar_weighted`(비용함수, 상태=(셀,진입방향)).
- `cost` — `RouteParams`/`clearance_map`/`CostModel`. 클리어런스는 admissibility 보호 위해 **가산 페널티**(감산 보너스 아님).
- `multi_route` (Step 1.4) — `route_sequential`/`order_tasks`. 깔린 경로를 점유로 추가해 충돌 없이 순차 라우팅. project6 실측 203/208(98%).
- `scene_io` (Step 1.5) — `scene.txt` 포맷 v1 동결(Phase 2/3 C++ 공유 계약). 섹션+TAB, mm, repr 실수·`\N` null → write/read 무손실. 점유/경로/방문 3레이어. `occupancy_from_doc` 로 점유 재구성.
- `viz` — PyVista 3D(점유/복셀격자/A* 경로/유틸별 다중경로/방문 레이어). `render_occupancy(visited=...)` 반투명 복셀, astar CLI `--visited`. 창 없이 스크린샷·HTML.
- `multi_route`/scenarios — Step 1.6 Numba **미채택**(프로파일상 A* 루프 ~100%지만 순수 파이썬 오버헤드 → 평면배열 포크 대신 Phase 3 C++ 로 이연). Step 1.7 회귀 시나리오 3종(`tests/scenarios/*`, `test_scenarios.py`) + `experiments/baseline_params.json` 확정.
- `docs/spec/*.docx` — Step 1.1~1.4 상세 설계 문서(구글독스용). 생성기 `python_experiments/out/_gen_spec_docs.py`(out/ 은 gitignore).

**Phase 1 완료(2026-05-28, DoD 5/5)**: 1.1~1.7 + scene.txt I/O + viz visited + 회귀 시나리오 3종 + baseline_params.json + Phase 2 입력 노트(`docs/phase2_input_notes.md`). pytest 203 통과. origin/main 푸시 완료.

**Phase 2 인터페이스 동결(명세 작성 완료, 2026-05-28)**: `docs/phase2_plan.md` + `docs/spec/{algorithm_spec, scene_format_spec, regression_set, performance_targets, freeze_signoff}.md`. 검토자 sign-off(freeze_signoff §4) 후 동결 확정.
- **성능 목표(사용자 확정)**: 최대 ~8,000m 스케일(OpenVDB+계층 corridor 필수) / 단일 배관 <1초 / 전체(수백 배관) <1분 / 메모리 <32GB.

**Phase 3 세부 계획서 작성 완료**(`docs/phase3_plan.md`): C++ 엔진(OpenVDB 희소 점유맵, Boost.Heap, abseil flat_hash_map, FCL, rip-up·CBS, pybind11). 빌드 제안=Windows MSVC C++20 + CMake + vcpkg(미확정). 마일스톤 C1~C10(코어→골든셋 검증→OpenVDB/성능→rip-up→리포트). 합격=골든셋 3종 재현 + 성능목표 달성.

**Phase 3 착수 — Step 3.1~3.5 + 3.9 + 3.10 완료(2026-05-28)**: `cpp/`(CMake+MSVC VS2022 Pro, C++20). `geometry/occupancy(Dense)/cost(clearance BFS+CostModel)/astar(균일+weighted 상태=(셀,방향))/multi_route/scene_io` 구현 + pybind11 바인딩. 테스트 ctest 3종(golden/scene_io/bindings) ALL PASS — 골든 01/02/03 **expanded_nodes 까지 Python 정확 일치**(01:22856/02:9036, 03:5/5성공·충돌0·총길이28050mm), `scenario_runner.py` 교차검증. 빌드: `cmake -S cpp -B cpp/build -G "Visual Studio 17 2022" -A x64; cmake --build cpp/build --config Release; ctest --test-dir cpp/build -C Release`. `cpp/build/` gitignore. MSVC 한글주석 위해 `/utf-8` 필수.
- **Step 3.5 multi_route**: `route_sequential`/`order_tasks`(안정 정렬=Python sorted 동일 결정성)/`mark_pipe`/`snap_to_free_cell`. `DenseOccupancy::copy()`(계약 M2 원본 불변, M1 충돌0).
- **Step 3.9 scene_io**(scene.txt v1): `dumps/loads/read/write_scene`+`occupancy_from_doc`. `RouteTask` 를 `route_task.hpp` 로 분리, 선택 문자열은 **std::optional**(None vs "" 구분=F3). **`format_repr_double`**=Python `repr(float)` 동일 표기(std::to_chars scientific→Python 임계값 재포맷, F4). 주의: `std::from_chars`(int)는 '+'지수를 거부 → 지수 파싱 전 '+' 제거 필수. 교차검증=Python 픽스처(`cpp/tests/fixtures/roundtrip.scene.txt`) 읽고 **바이트 동일 재출력**(F2). `.gitignore` 의 `*.scene.txt` 무시를 `!cpp/tests/fixtures/*.scene.txt` 로 예외, 픽스처 LF 고정 `.gitattributes`.
- **Step 3.10 pybind11**: 모듈 **`routing3d_cpp`**(`cpp/bindings/bindings.cpp`). pybind11 3.0.4 = .venv pip 설치(header-only). CMake `-DBUILD_PYTHON_BINDINGS=ON`(기본 OFF) + `-Dpybind11_DIR=$(python -c "import pybind11;print(pybind11.get_cmake_dir())")` + `-DROUTING3D_TEST_PYTHON=<.venv python>`. pybind11 이 classic FindPythonInterp 로 C:\Python313(=venv 베이스, 3.13) 발견 → `.pyd`(cp313)는 venv 에서 import 가능. 교차검증 `cpp/bindings/test_bindings.py`(ctest `bindings`): 바인딩 경유 골든 01/02 경로셀까지 일치, 03·scene.txt 왕복 일치.
- **Step 3.6 (part 1·2·3) 점유 백엔드 + OpenVDB + 엔진 무관화**: 좌표/복셀화를 `geometry.hpp` 공유함수(`grid_in_bounds`/`grid_cell_to_world`/`grid_world_to_cell`/`grid_box_range`)로 일원화 → 백엔드 불문 동일 결과(불변식 O1). `SparseOccupancy`(64비트 패킹키 해시셋, 메모리=O(점유셀)). `VdbOccupancy`(OpenVDB BoolGrid, pimpl). **vcpkg=`D:/vcpkg`**(bootstrap 완료), **OpenVDB 12.0.1**+TBB 설치(~18분). CMake `-DUSE_OPENVDB=ON`+`-DCMAKE_TOOLCHAIN_FILE=D:/vcpkg/scripts/buildsystems/vcpkg.cmake -DVCPKG_TARGET_TRIPLET=x64-windows` → `routing3d_vdb` lib(OpenVDB DLL 은 vcpkg applocal 로 exe 옆 자동복사). **엔진(astar/cost/multi_route)을 헤더 전용 템플릿으로 전환**(`Occ` 백엔드 무관, .cpp 3개 삭제, `count_turns` 인라인, `MultiRouteResult<Occ>`). 교차검증: 골든01/02 가 Dense=Sparse=Vdb 지표+경로 동일, 골든03 도 셋 다 5/5·28050mm·충돌0. 테스트 ctest 5종 ALL PASS. **OpenVDB 타일 압축 한계**: 얇은 시트(한 축<노드dim 128)는 leaf 타일까지만 → 8,000m 바닥/천장은 여전히 무겁다(전역 fine 격자 비현실적). 해결=계층 coarse + 로컬 corridor(미구현). 또한 현 A* 는 g/closed 를 lin() 선형배열로 잡아 '경계 한정' 격자용 → 초대형 전역 탐색은 해시 기반 확장 필요.

- **Step 3.6 (part 4) 계층 corridor + 해시 A***(`corridor.hpp`): `astar_hashed`(g/closed 를 20비트 패킹키 해시맵 → occ.size() 배열 미할당, 초대형 격자 가능; corridor 술어로 탐색제한; 전체허용 시 배열 astar 와 동일 경로/확장수). `route_corridor`(coarse 가이드→반경 tube 팽창→fine A* tube 한정). 검증: 8,000m(160000³) 로컬 배관 ~75ms, 벽 우회 corridor 가 전역 최단 동일 길이·확장 55%. ctest 6/6. 한계: 해시경로 클리어런스 비활성·coarse 호출자 구성. **→ Step 3.6 완료(part 1~4).**

- **Step 3.7 FCL 정밀 충돌**(`fcl_scene.hpp/.cpp`): 장애물 AABB→FCL dynamic-AABB-tree broadphase, 50mm 복셀 넘는 정밀 질의(`collides_point/sphere`/`segment_clear`=반경r 캡슐/`path_clear`). FCL/Eigen pimpl. **FCL 0.7.0** vcpkg(~2.6분, +eigen3/ccd/octomap). CMake `-DUSE_FCL=ON` → `routing3d_fcl` lib. 검증: FCL점=AABB=점유맵, 틈(200) 가는파이프(r50) 통과·굵은(r150) 충돌(sub-voxel). ctest 7/7.

- **실행 진입점(CLI)**: `cpp/cli/routing3d_cli.cpp` → `routing3d_cli`(코어만, 외부의존성 불필요). 명령: `demo`(내장 골든03 5배관), `route --in/--out [--mode multi|single] [--priority]`, `summary --in`. 결과를 scene.txt(경로 레이어 포함)로 출력. 루트 **`run.ps1`** 이 빌드+실행 래핑(인자 없으면 demo). 윈도우 한글출력 위해 CLI 는 `chcp 65001`.

- **C# 인터롭(네이티브 DLL)**: `cpp/capi/routing3d_capi.{h,cpp}` → `routing3d_capi.dll`(코어만 링크=외부의존성 없는 단일 DLL 261KB, `-DROUTING3D_CAPI_EXPORTS`, CMake `add_library(... SHARED)`). C ABI(extern "C", cdecl) 2레벨: **Level1 문자열** `r3d_route_scene_text`(scene.txt in→out, mode multi|single), **Level2 핸들** `r3d_create/destroy/load_scene_text/set_grid/set_params/add_obstacle/add_task/set_task_endpoints/route_multi/route_task/get_result/copy_path/dump_scene_text`. 불투명 핸들 `R3dEngine`=SceneDoc 보유, 예외 비전파(try/catch→`R3dStatus`), 콜리할당 문자열=`r3d_free_string`(malloc/free 동일 DLL), 경로=콜러할당 2단계(`R3dResult.path_len`→`copy_path`), POD blittable 구조체, **문자열 UTF-8**(한글). `route_multi` 는 원본 인덱스 보존 위해 route_sequential 빌딩블록(order/snap/astar_weighted/mark_pipe) 재구현. 스모크 ctest `capi`=DLL경유 골든03 5/5·28050mm + 문자열왕복. **ctest 8/8**. 설계서 `docs/csharp_helix_interop_design.md`(C ABI 2레벨 / C# P/Invoke+SafeHandle+Engine 래퍼 / HelixToolkit WPF 뷰어 매핑 / CMake SHARED·x64·런타임DLL / 로드맵 P0~P3 / UTF-8·비트·예외누수 리스크). 사용자 선택=네이티브 DLL 인터롭.
- **결과 가시화(Python, C++ 산출물용)**: `routing3d_py/viz_scene.py` → C++ `routing3d_cli` 가 쓴 scene.txt 를 기존 `viz.render_occupancy` 로 렌더(장애물 반투명 + 유틸리티별 경로 튜브, `utility_colors` 동일 규약). CLI `python -m routing3d_py.viz_scene --in <scene.txt> [--screenshot/--html/--show/--utility/--visited]`. PNG·인터랙티브 창 동작, **HTML 은 `trame_vtk`(pip install "pyvista[jupyter]") 필요**(기존 viz.py 와 동일 한계).
- **통합 개발보고서**: `docs/routing3d_dev_report.{docx,pdf}`(전체+단계별 Phase 1~3, 흐름도/알고리즘/함수·변수/결과). 생성기 `python_experiments/out/_gen_dev_report.py` + `_docx_to_pdf.ps1`(MS Word COM). `python_experiments/out/` 은 `.gitignore` 예외(`!.../*.py`,`*.ps1`)로 생성기 소스만 추적.

- **C# 뷰어(P1 완료)**: `csharp/Routing3D.Viewer/`(WPF, .NET 9, **net9.0-windows, x64 고정**). DLL `routing3d_capi` 를 **P/Invoke**로 호출해 라우팅하고 HelixToolkit 으로 3D 렌더(엔진=C++, 뷰어=C#). 구성: `Interop/`(Native[P/Invoke, 문자열 UTF-8 byte[]]·R3dEngineHandle[SafeHandle]·Engine[OOP 래퍼]), `Model/`(SceneData·SceneTextParser·UtilityColors=Python utility_colors 동일), `ViewModels/SceneViewModel`(장애물 반투명 박스 머지 + 유틸별 경로 튜브, 시작/끝 구), `MainWindow`(툴바: 내장데모/scene.txt열기/재라우팅 + 범례 + 상태바). **HelixToolkit.Wpf 2.24.0**(WPF 네이티브 MeshBuilder/MeshGeometry3D; **3.x 는 geometry 가 System.Numerics 로 분리돼 WPF 변환 번거로워 미채택**). 빌드 `dotnet build csharp/Routing3D.Viewer.sln -c Release`(routing3d_capi.dll 자동복사), 실행 `dotnet run --project csharp/Routing3D.Viewer -c Release`(시작 시 내장 골든03 자동 라우팅·표시). 검증: 앱 기동 시 P/Invoke 로드+데모 route_multi+copy_path+렌더 크래시 없음 확인(GUI 시각검증은 데스크톱 필요). `.gitignore` 에 `[Bb]in/`,`[Oo]bj/` 추가. sln=`csharp/Routing3D.Viewer.sln`.

- **C# 뷰어 P2(인터랙티브 재라우팅) 완료(2026-05-29)**: 우측 패널 작업목록(ListBox, 유틸색+성공/길이) + 선택 배관 종단점(시작/끝 XYZ) 편집기. **단일 재라우팅**(`Engine.RouteTask`=`r3d_set_task_endpoints`+`r3d_route_task`, 원본 장애물 기준) / **전체 재라우팅**(`route_multi`). 추가: `ObservableObject` 기반클래스, `TaskRowVM`(편집가능 종단점+결과), `Engine.RouteTask(int)`. 편집값은 SyncEndpoints 로 SceneData+엔진 반영 후 라우팅→RefreshResults→BuildModel. 빌드 클린, 기동 검증 OK(클릭 인터랙션 시각검증은 데스크톱 필요).

- **C# 뷰어 P3a(뷰어 기능) 완료(2026-05-29)**: 충돌 시각화(`Model/CollisionFinder` = 경로 셀 공유 탐지 → 빨간 큐브, 단일 재라우팅 후 겹침 확인용; multi 는 0), 표시 토글(ShowObstacles/ShowPaths/ShowCollisions 체크박스 → BuildModel 재구성, 라우팅 안 함), 3D 클릭 종단점 지정(`PickMode` Start/End + 코드비하인드 `HelixViewport3D.FindNearestPoint`→셀 중심 스냅→SelectedTask 종단점). 빌드 클린·기동 검증 OK. **P3b(엔진, 후속)**: 대형장면 OpenVDB/corridor 를 C ABI 로 노출 + `routing3d_capi` USE_OPENVDB 빌드(+런타임 openvdb/tbb DLL 동봉) — 미착수.

- **C ABI corridor(P3b) 완료(2026-05-29)**: `r3d_route_corridor(e, factor, radius)` 추가. 장애물을 **fine/coarse SparseOccupancy** 로 만들고 작업별 `route_corridor`(coarse 가이드→fine tube, `astar_hashed` 해시 기반). **occ.size() 배열 미할당 → 초대형 격자 동작, OpenVDB 불필요 → DLL 코어 전용 유지**(의존성 0). 작업별 독립(충돌 회피 없음, 균일 비용). ctest `capi`: 2000²×8 Sparse 빈 장면 corridor=성공·길이 198000(=맨해튼)·확장 274,351(Dense weighted A* closed 배열 2.2e11 불가). C#: `Engine.RouteCorridor` + 뷰어 "corridor 라우팅(대형/독립)" 버튼(데모 좁은통로에선 5개 겹쳐 충돌 큐브 표시=충돌viz 데모). 선택 보류=VDB 백엔드 capi(USE_OPENVDB+런타임DLL 동봉; Sparse 로 충족돼 미착수).

- **C# 뷰어 P3c(검증 모드) + 실데이터 3자 교차검증 완료(2026-05-29)**: 뷰어 **명령행 인자 로드**(`scene.txt` 경로 인자 → 데모 대신 그 장면; `App.OnStartup` 처리, `StartupUri` 제거) + **헤드리스 셀프테스트**(`Routing3D.Viewer.exe --selftest <scene> <out>`: 창 없이 파싱+P/Invoke 라우팅+모델 구성 후 `SceneViewModel.Status` 를 파일로 기록). 실 DB 장면(AUTOROUTINGV7 project6, 장애물 983·작업 208)으로 교차검증: **동일 cell=100 파일에서 Python(`route_sequential`)·C++ CLI(`route_multi`)·C# 뷰어(`--selftest`) 모두 194/208·총 3,400,800mm 완전 일치**(cell=200 동일파일은 셋 다 77/208·1,532,800mm). 격자 해상도로 성공수 달라지므로 **같은 파일끼리** 비교해야 함. 커밋 `377f3b9` origin/main 푸시. 주의: `App.xaml` 주석에 `--`(XML 주석 금지문자) 넣지 말 것(MC3000).

- **Step 3.8 rip-up & reroute 완료(2026-05-29)**: `route_ripup`(multi_route.hpp 템플릿 + multi_route.py 레퍼런스, 1:1). **무손실 결정적** 알고리즘 — 실패 배관 f 의 '장애물만' 이상 경로가 가로지르는 기존 배관(blocker)을 뜯어내고 f 배치 후 blocker 전부 재라우팅, **f 성공 + blocker 전부 재배치 성공일 때만 채택(성공 수 단조 +1)**. `order_indices` 헬퍼 추가(order_tasks 를 그 위에 재구현, 출력 동일). 노출: CLI `route --mode ripup`, C ABI `r3d_route_ripup`(원본 작업 인덱스 보존 — order_indices 로 ordered position→원본 매핑). 검증: 합성 혼잡(9×9 벽+틈2개) **순차 1/2 → rip-up 2/2**, LONG 1000→1300mm 우회·SHORT 900mm — **C++ test_ripup(ctest `ripup`) = pytest test_ripup_resolves_congestion 동일 값(교차검증)**. ctest **9/9**, pytest 11. **한계/효과**: 무손실 greedy 라 '혼잡'만 해소, '접근불가'(종단 PoC 가 장애물에 파묻힘)는 구조상 불가. **격자 해상도별 효과 차이(회귀 리포트 측정)**: project6 **cell=200 은 rip-up 77→80(+3) 실제 개선**(거친 격자=혼잡 많음), **cell=100 은 194=194 동일**(잔여 실패가 접근불가 성격). 즉 rip-up 은 실데이터에서도 혼잡이 있으면 효과 있음. 후속 개선=PoC 전처리/스냅 확장 또는 비용기반 negotiated-congestion/CBS. 임시 검증 스크립트(_ripup_*.py)는 테스트로 대체 후 삭제.
- **개발보고서 5장 추가(2026-05-29)**: `docs/routing3d_dev_report.{docx,pdf}` 에 'C++↔C# 인터롭'(C ABI DLL 5.1 / HelixToolkit 뷰어 P0~P3c 5.2 / 실데이터 Python=C++=C# 194/208 교차검증 5.3) + 4.5 rip-up 절 + 결론(6장) 추가. 생성기 `_gen_dev_report.py` 에 `ch_csharp` 챕터.
- **Step 3.12 회귀 리포트(2026-05-29)**: `docs/routing3d_regression_report.{docx,pdf}` — 표준 벤치셋을 **실제 실행·측정**해 기대치와 비교. 생성기 `python_experiments/out/_gen_regression_report.py`(골든 3종=Python scenario_runner+expected_metrics 비교 / rip-up 합성 / project6=C++ CLI subprocess 측정+타이밍). 결과: 골든 3/3 PASS, rip-up 합성 1/2→2/2, project6 cell=100 multi/ripup 194/208(~637ms/배관), **cell=200 multi 77→ripup 80(+3)**. CLI/scene 없으면 해당 절 건너뜀. 재생성은 project6 cell=100 4케이스라 ~5분.
- **C# 뷰어 SpaceAI 다크 UI 리스타일(2026-05-30)**: SpaceAI(`..\SpaceAI\routing_viewer_wpf\MainWindow.xaml`)와 동일 팔레트(#1e2230/#252b3d/#2b3548/#404a64, 강조 #385b85)·3-컬럼 레이아웃으로 [MainWindow.xaml](csharp/Routing3D.Viewer/MainWindow.xaml) 전면 재작성. 신규 기능: **① ↺ 전체보기**(`FitViewCommand`→`FitViewRequested` 이벤트→`View.ZoomExtents`), **② 객체 보이기/숨기기**(기존 레이어 토글=장애물/경로/충돌 + 좌측 **유틸리티별 체크박스 리스트**[전체 선택/해제], `UtilityFilterVM.IsVisible` 변경 시 `TasksView.Refresh()`+`BuildModel()` 으로 작업 목록·3D 동시 반영), **③ 라우팅 경로 검색**(좌측 SearchText 부분일치·대소문자 무시, `TasksView`=`ICollectionView` 필터=검색일치 AND 유틸리티 IsVisible, 'X / Y' 카운트). 신규 [UtilityFilterVM](csharp/Routing3D.Viewer/ViewModels/UtilityFilterVM.cs)(Label/Swatch/Count/IsVisible). SceneViewModel 확장: SearchText·TasksView·UtilityFilters·TaskCountText·FitViewCommand·UtilityAll/ClearCommand·OnUtilityFilterChanged. 검증: --selftest project6 cell=100 194/208 그대로(회귀 없음), GUI 6s 기동 OK. 커밋 `10b7586`.
- **엔진 visited 셀 수집 + 3D 신규 레이어 3종(2026-05-30)**: 뷰어 '방문맵·점유맵·복셀 전체맵' 가시화를 위한 엔진~뷰어 전 구간 확장. **엔진**: AStarResult.visited 필드 + astar/astar_weighted 의 collect_visited 옵션(기본 false). 가중 A* 는 셀 단위 visited_seen 비트맵으로 중복 제거. route_sequential/route_ripup 이 인자 전파. **C ABI**: R3dEngine.collect_visited(기본 on) + r3d_set_collect_visited 토글, R3dResult.visited_len, r3d_copy_visited(task, …) / r3d_copy_blocked(buf=NULL,0 → 총 카운트 조회). to_scene_result 가 SceneResult.visited 도 복사. pybind11 바인딩에 collect_visited 인자 + AStarResult.visited 노출. **C# 뷰어**: RouteResult.Visited / Engine.CopyBlocked / SetCollectVisited 래퍼. SceneViewModel 에 토글 3개(ShowGridFrame · ShowOccupancyVoxels · ShowVisitedMap) + 헬퍼 AddGridFrame(12 변 가는 실린더)·AddOccupancyVoxels(셀당 큐브, 50k 초과 시 균등 다운샘플)·방문맵 유틸리티별 머지 메시(유틸 색 alpha≈31%, 유틸당 12k 셀 다운샘플). 유틸리티 필터 IsVisible 가 방문맵에도 적용. MainWindow.xaml WrapPanel 에 체크박스 3개. 검증: ctest 9/9, --selftest project6 cell=100 그대로 194/208·3,400,800mm. 커밋 `61e2a6f`(엔진/capi/bindings) + `6392908`(C# 뷰어 레이어).
- **C# 뷰어 DB 자동 로드(SpaceAI 스타일, 2026-05-30)**: 인자 없이 실행 시 AUTOROUTINGV7 에 자동 접속 → 프로젝트 목록 → 첫 프로젝트의 장애물·PoC 페어 로드 → 라우팅 → 전체보기. **신규 [ObstacleDbLoader](csharp/Routing3D.Viewer/Model/ObstacleDbLoader.cs)**(Npgsql 8.0.4): `DbConfig`(host/port/db/user/password, PGHOST 등 env 우선) · `ProjectInfo`('[id] proc/eq — src' 표시) · `ListProjects()`(space_project_map ORDER BY project_id) · `LoadScene(projectId, cellMm)`(TB_BIM_OBSTACLES AABB + TB_BIM_EQUIPMENT IS_MAIN 의 POC_LIST jsonb 를 System.Text.Json 으로 파싱해 작업 페어 생성, connectedOnly 기본 true, 격자 origin=lo·shape=ceil((hi-lo)/cell)). **SceneViewModel**: DbHost/Port/User/Password/Database/CellMm 바인딩 + Projects 컬렉션 + SelectedProject 의 set 이 자동 LoadFromDb → 엔진 적재 → RouteMulti → SceneRebuilt → ZoomExtents. 생성자에서 scene.txt 인자 없으면 LoadProjects() 호출, DB 실패 시 LoadDemo() 폴백. **MainWindow.xaml 상단 툴바**: SpaceAI 동일 팔레트로 DB 5필드(host/port/user/pwd/db) + 프로젝트 콤보 + ↻ 다시 로드 + 셀 + ▶ 재라우팅/🌐 corridor/↺ 전체보기/📁 scene.txt/🧪 데모. 검증: 빌드 클린, GUI 12s 기동(DB 접속 + 폴백 양 경로 통과). 커밋 `b9a93b9`.

- **Step 3.11 sparse 확장(정밀 셀 대응) 완료(2026-06-01, 브랜치 `feat/existing-vs-developed-compare`)**: 셀을 줄이면 셀 수가 부피로 폭증해 (점유배열 25mm 130MB·10mm 2GB) + (클리어런스 거리맵 vector<int>(N) 520MB) + (A* `lin()` int 키 10mm 오버플로)가 동시에 터지던 문제를 근본 해결. **신규 [box_index.hpp](cpp/include/routing3d/box_index.hpp)** `SpatialBoxIndex`(장애물 AABB를 유니폼그리드 broadphase로 색인, 복셀화 안 함 → 메모리 O(장애물), 셀 크기 무관; 반열린 중첩=Dense 복셀화와 동일 점유 O1). **신규 [ImplicitOccupancy](cpp/include/routing3d/occupancy.hpp)**(박스색인 + 깔린배관 해시셋, **64비트 lin/unlin**, **온디맨드 clearance_cells**=박스 최근접 질의). `astar_weighted` state_of/unlin 64비트화(`state_of(long long)`). `CostModel` 에 `HasClearanceQuery` 컨셉 + `if constexpr` → Implicit 은 전역 거리변환 미생성. capi `route_multi`: **격자>5M셀이면 Implicit 자동 전환, 이하 Dense**(골든 바이트 불변). Dense/Sparse `unlin(int)`→`unlin(long long)`. 검증: **ctest 10/10**(신규 `test_implicit` = Dense==Implicit is_blocked 전수 일치 + 10mm 20.3억셀 오버플로/OOM 없음), 헤드리스 `--dbroute 1 25 ALL` = **113/113**(clearance ON, 이전 108/113↑), `--dbroute 1 10 ALL` = exit 0(이전 exit 255 크래시 해소). **남은 병목=탐색량**: 10mm 는 ~110s/배관·탐색상한(12M) 도달분 실패(메모리 아님) → 다음 계층=계층 corridor 강건화(굵은 가이드→가는 튜브, 종단점 최근접 스냅). 커밋 `8bdcda0`.
- **헤드리스 라우팅 진단 도구**: `Routing3D.Viewer.exe --dbroute <projectId> <cellMm> <utility|ALL> <out>`(GUI 없이 DB 로드+facilities+drop+route, [DbRouteDiag.cs](csharp/Routing3D.Viewer/Diagnostics/DbRouteDiag.cs)).
- **라우팅 진행 다이얼로그 + 자동설계 삭제(2026-06-01)**: 그룹/유틸 전체 라우팅 시 [RoutingProgressWindow](csharp/Routing3D.Viewer/Views/RoutingProgressWindow.xaml) 자동 표출 — 컬럼 순서·유틸리티그룹·유틸리티·시작PoC·종단PoC·**처리상태(엔진 단계 진행율%: 대기→탐색N%→완료/실패)**·추정사유·길이·꺾임·확장수·ms. 배관 완료마다 **3D 라이브 갱신**(`LiveRouteModel` Model3DGroup, 유틸색 튜브 점진 추가). 기반=C ABI **`r3d_route_multi_progress`**(cdecl 콜백, phase 0=탐색진행/1=완료, progress01, 경로 셀 전달) + astar_weighted `on_progress`(휴리스틱 근접 기반 %, 5만 확장마다, 결과/결정성 불변). 콜백은 백그라운드 스레드 → 메타/유틸색은 UI 스레드에서 선계산. **🗑 자동설계 경로 삭제 버튼**(`ClearRoutesCommand`=전체 결과 초기화) + RouteRowsAsync 가 **재설계 시 대상 행 기존결과 먼저 초기화**(동일 Start PoC 재설계 시 옛 경로 안 남음, 대상 외 누적은 보존). **AddFacilityObstacles 종단 제외를 '전체 작업' 기준**으로(부분집합도 113/113 동일 제외). 상태바=배치결과("그룹 완료 N/N") 앞세움(전체 누적 16/113 오해 제거). 커밋 `a2805bb`/`0dae8f0`/`82fef0d`.
  - **진단 사례**: GUI 'Exhaust 그룹 16/113' 은 실패 아님 — Exhaust(=ALKA 14+ORG 2)=**16작업 전부 성공(16/16)**, 113은 전체 씬 총수. 상태바 표기 오해였음.
  - **알려진 사전 결함(본 작업 무관)**: pybind `cpp/bindings/bindings.cpp:134` 의 `astar_weighted` 바인딩이 `py::arg` 개수 불일치(커밋 2ce3eb8 의 corridor 인자 + 이번 on_progress 인자 미반영) → **파이썬 모듈(BUILD_PYTHON_BINDINGS) 빌드만 실패**, capi/뷰어/ctest 무관. 파이썬 바인딩 다시 쓸 때 `py::arg` 보강 필요.

- **덕트 관통 수정(2026-06-01, 커밋 `9882bad`)**: 종단 PoC 포함 덕트를 장애물에서 제외하던 로직 제거 → **설비/덕트/레터럴 항상 솔리드** + **LiftPocToSurface**(PoC를 솔리드 표면 바깥 +½셀 투영, 덕트는 상부 우선) + **DropStartBelowEquipment**(장비 출발 아래로) + **가중 A*(`w_heur`, 대형격자 1.5)**. ALKA 25mm 14/14·2.6s, 관통 0. 이 네 규칙이 곧 패턴학습의 '기하 기본 스텁'.
- **기존설계 패턴 학습 L1 완료(2026-06-01, pgvector)**: 사람이 그린 기존배관(TB_ROUTE_PATH)의 양 끝 '스텁'(장비 출발=EQUIP / 덕트·레터럴 진입=DUCT)을 학습해 자동라우팅에 활용하는 기능. DB에 **pgvector/cube/postgis 설치 확인됨**. **신규 [db/schema/route_stub_pattern.sql](db/schema/route_stub_pattern.sql)**=벡터 저장소(`feat vector(24)`[face 1hot6+1차방향6+2차방향6+앵커내상대좌표3+시작→종단단위3], `dir_unit vector(3)`, HNSW+btree 인덱스) + 집계뷰 `route_stub_template`(키별 mode(face/dir_seq)·median(rise/offset)). **신규 Python 모듈 3개**: [route_db.py](python_experiments/routing3d_py/route_db.py)(기존배관 3-join·장비·덕트 로더, C#→Python 포팅 — Python이 처음으로 TB_ROUTE_PATH 접근) · [pattern_learn.py](python_experiments/routing3d_py/pattern_learn.py)(스텁 추출→앵커AABB 로컬프레임 정규화→특징벡터; `STUB_MAX_MM=4000`·`STUB_MAX_BENDS=3`) · [pattern_db.py](python_experiments/routing3d_py/pattern_db.py)(스키마적용·벡터적재[psycopg2가 vector를 모르므로 '[a,b,c]' 텍스트 리터럴 바인딩]·`nearest_stubs`=범주 pre-filter 후 `feat <-> q` ANN). CLI `python -m routing3d_py.pattern_learn --project N {--report|--write-db}`. **검증**: project6 → 405표본/38키 적재, **학습이 도메인규칙을 입증**(EQUIP face=−z 지배, DUCT face=+z 지배 = 덕트 상부진입), pytest 9/9(test_pattern_learn). **남은 단계**: L2a(C#만, 학습 face+rise로 PoC 투영 일반화=LiftPocToSurface 학습판, 엔진변경0) → L2b(엔진+C ABI `r3d_set_corridor_cells` 신설+w_corridor 소프트 바이어스) → L3(하드 스텁 구간연쇄·랙 번들·검색증강 ANN). 사용자 선택=L2a→L2b 순차. **개발계획 문서**: [docs/routing3d_pattern_learning_plan.{docx,pdf}](docs/routing3d_pattern_learning_plan.docx)(생성기 `python_experiments/out/_gen_pattern_learning_plan.py`).

- **패턴학습 L2a 완료(2026-06-01, C#만·엔진변경0)**: 학습된 진출/진입 면으로 PoC 투영 — **신규 [Model/PatternStore.cs](csharp/Routing3D.Viewer/Model/PatternStore.cs)**(Npgsql로 `route_stub_template` 조회 → (kind,group,util)→(face,rise), 폴백 (kind,group)→(kind)). **SceneViewModel**: `_patterns`(LoadFromDbAsync 1회 로드·미스캐시) + `UsePatterns` 토글(기본 ON) + `PatternStatus` 라벨 + `LearnedFace(kind,group,util)`. **LiftPocToSurface 에 `preferFace` 인자** — 최근접 면 대신 학습 면으로 탈출(없으면 기존 최소침투). BuildEngineForRows 가 시작=EQUIP·종단=DUCT 학습면 적용. MainWindow.xaml 체크박스(PatternStatus 바인딩). DbRouteDiag 도 PatternStore 사용(env `R3D_PATTERNS=off` 로 A/B). **A/B 실측(project6 c100 ALL)**: OFF 185/208·23.6s(한 배관 675만 노드 헛탐색) → **ON 187/208·6.4s·총길이↓·헛탐색 0**(3.7× 빠름, +2 성공, 회귀 0). 남은 21실패=전부 expanded=0(PoC가 솔리드에 파묻힘=접근불가, 면투영으로 해소 불가 → L3/전처리). 빌드 클린.
- **접근불가 PoC 전처리 완료(2026-06-02, C#만)**: L2a 잔여 실패(전부 expanded=0 = PoC가 솔리드에 파묻혀 투영조차 안 됨)를 직접 해소. **SceneViewModel `SnapPocToFreeCell(x,y,z,preferFace)`** — Lift 후에도 셀이 막혀 있으면 ① 학습면 법선으로 바깥 행진(최대 16셀) → ② 체비셰프 반경 확장(최대 6셀)으로 최근접 자유셀로 이동. `CellBlocked`(장애물 비통과+설비/덕트, minT 팽창, box↔cell overlap = 엔진 복셀화 근사) + `InGrid` + `FaceNormal` 헬퍼. BuildEngineForRows 가 시작/종단에 적용. 이미 자유면 그대로(회귀 0). DbRouteDiag 도 동일(env `R3D_SNAP=off` A/B). **A/B 실측(project6 c100 ALL, 패턴 ON)**: 스냅 OFF 187/208(실패 21 전부 expanded=0) → **스냅 ON 194/208**(+7, 총길이↓ 1,442,800). 남은 14실패는 이제 expanded>0(끝점은 풀림, 경로 없음=혼잡/막힘=rip-up/CBS 영역, 접근불가 아님). **전체 누적(project6 c100): 무패턴·무스냅 185/208·23.6s → 패턴+스냅 194/208·6.7s**(+9, 3.5×). L2b(회랑 소프트바이어스)는 **보류**(cost.hpp corridor 키가 int 절단이라 대형격자 64비트화 필요 + 골든 회귀 위험, ROI는 이미 성공경로의 모양 개선뿐 — 실패감소 아님). 사용자 선택=전처리 우선.
- **패턴학습 L2b 완료(2026-06-02, 커밋 `5eae5a8`)**: 기존설계 회랑 소프트 바이어스. **엔진**: corridor 키 `unordered_set<int>`→`unordered_set<long long>`(occ.lin) 확장(cost.hpp/astar.hpp/multi_route.hpp/test_attract) → ImplicitOccupancy 64비트 격자 절단 해소. **C ABI**: `r3d_set_corridor_cells(e,ijk,n)` 신설 + R3dEngine.corridor_seed + route_multi_impl/into_doc 가 시드 받아 w_corridor>0 일 때 회랑에 occ.lin 으로 미리 넣음(깔린배관 번들링과 병행). **C#**: Native/Engine `SetCorridorCells` + SceneViewModel `UseDesignCorridor` 토글(기본 OFF) + `BuildDesignCorridorCells`(매칭 기존배관 폴리라인을 셀 복셀화 ±2, FindMatchingExistingPipe 재사용) + SetParams w_corridor=½칸·corridorRadius 2. MainWindow 체크박스. DbRouteDiag A/B(env `R3D_CORRIDOR=on`, MatchPipe). **검증**: ctest golden/capi/implicit/attract/corridor 5/5 불변(회랑은 w_corridor>0 일 때만). **A/B(project6 c100 ALL)**: 회랑 OFF 194/208·6.5s → **ON 198/208·46s·총길이 1,458,300**. ON=+4성공(설계 채널 유도)·총길이↑(설계 정합)·~7× 느림(w_corridor 페널티, 2건 탐색상한 근접). 옵트인. 향후: w_corridor 튜닝·회랑 폭 적응으로 속도 개선.

**현재 상태(2026-06-02)**: Phase 3 = 3.1~3.12 완료. **C# 인터롭 P0~P3i + 진행다이얼로그 + 자동설계삭제 + 덕트관통수정 완료**. **기존설계 패턴학습 L1(pgvector) + L2a(학습면 투영) 완료·실측 검증 → L2b(회랑 소프트바이어스) 착수**. 작업 브랜치 **`feat/existing-vs-developed-compare`**(origin 푸시됨, main 미병합, **패턴학습 커밋 미완료** — L1/L2a 변경 스테이지 안 됨). **남은 선택지**: 패턴학습 L2b/L3 / 정밀셀 10mm 탐색량 / 접근불가 PoC 전처리(L2a 잔여 실패 해소) / CBS.

**문서 산출물(2026-05-30 갱신)**:
- `docs/routing3d_dev_report.{docx,pdf}` — 전체 + 단계별 개발보고서(생성기 `python_experiments/out/_gen_dev_report.py`)
- `docs/routing3d_regression_report.{docx,pdf}` — 표준 벤치 자동 측정 리포트(생성기 `_gen_regression_report.py`, 재생성 ~5분 = project6 cell=100 4케이스)
- `docs/csharp_helix_interop_design.md` — C ABI/뷰어 설계 + 로드맵 P0~P3f
- `docs/spec/*.md` — Phase 2 동결 명세(algorithm/scene_format/regression_set/performance_targets/freeze_signoff)
- `docs/phase{1,2,3}_plan.md`, `docs/development_plan.md` — 마스터 계획

**git(2026-06-02 갱신5)**: PR #1~#10 모두 main 머지 완료. 현재 **main = origin 동기**(PR #10 머지 `c53769a` 이후 스텁 장애물 등록 커밋 추가). PR 워크플로: 브랜치→GitHub API(credential fill 토큰)로 PR 생성+머지→main ff→브랜치 삭제. 신규 작업(갱신4 이후):
- **스텁=수직배관+엘보(PR #8)**: `_walk_stub` 재작성(`_dir_runs`+`_merge_short_runs` 250mm 지터 흡수+엘보 포함, `STUB_LEADIN_MM=800`). EQUIP `-z,+x`·DUCT `+z,-x`. 재학습 405건. 엘보 방향이 feat dir2 인코딩.
- **뷰어 기능**: 좌측 유틸그룹 선택→기존배관 그룹 필터(PR #3) · 시작/종단 PoC 마커 초기표시(PR #4) · 기존배관 출발(빨강)/종단(파랑) 스텁 표시(PR #9, `Model/StubExtractor.cs`=`_walk_stub` C# 포팅).
- **스텁 라우팅(PR #10)**: 매칭 기존배관 스텁을 고정 설계구간으로 깔고 A* 는 스텁끝(랙)~끝만 탐색. 표시=출발스텁+A*중간+종단스텁. `BuildEngineForRows`+`TaskRowVM.{StartStub,EndStub}`+UI '스텁 라우팅'(기본 ON)+env `R3D_STUB`. **199→206**(스텁 208/208 매칭, 랙↔랙 자유공간 탐색 쉬움)·랙집중 17→37%. **스텁 장애물 등록**(`AddPolylineObstacle`, 세그먼트-AABB 메모리효율)로 교차배치 충돌 회피.
- **문서**: 스텁 기술레퍼런스 `docs/routing3d_stub_pattern` + 실측 Exhaust 워크드예시 `docs/routing3d_stub_example_exhaust`(matplotlib 3D 그림 포함, 생성기 `_gen_stub_*.py`).
- **권한**: `.claude/settings.local.json` 에 `Bash(*)·PowerShell(*)·Read/Edit/Write/Glob/Grep(*)` blanket 허용(사용자 요청, 다음 세션부터 적용). 종료시 메일 금지([[feedback-no-email]]).

직전 상태(참고): PR #2 머지 시 main = `d5aed7c`. 작업 브랜치 모두 삭제. 로컬 잔여 `backup-local-pre-sync`(미정리). gh CLI 없어 **git credential fill→GitHub API**로 PR 생성/머지. **L3b 교훈**: 무분별 ANN 면 분기는 해로움(199→192, UPW_S/HOT DI_S 의 [rel,dir]이 면과 반상관) → **자기검증 LOO 게이트**(ANN 이 집계 다수결을 +10pp 이기는 키만 적용)로 NFW 1키만 채택·회귀 0. **Npgsql 함정**: 연결당 reader 1개 — 첫 reader 안 닫고 둘째 열면 TryLoad 통째 null(패턴 전체 비활성). 헤드리스 A/B env: `R3D_PATTERNS/SNAP/CORRIDOR/RACK/ANN`(off) + `R3D_WCORR/WHEUR/CORRRAD`(튜닝). **L2b 속도 튜닝 완료**(`perf(viewer)`): weighted A* `w_heur` 1.5→2.0 + 임계 5M→300k셀 → **회랑 ON 47s→6.2s(7.6×)·198→199**, 회랑 OFF 도 194→199. 엔진 무변경(뷰어 파라미터). **L3 완료**: L3a 랙번들(유틸그룹 수평런 z-높이 학습→`rack_levels`, `learn_rack_levels`+C# `BuildRackLevels`+UI '랙 번들(L3a)', **199→200·랙집중도 17→21%**, 랙 페널티 cell×0.2) + L3b 검색증강(`pattern_db.suggest_stub` HNSW K-NN 투표 프리미티브+DB테스트). pytest 243. 진단 env 추가: `R3D_WCORR/WHEUR/CORRRAD/RACK`. 직전: L1+L2a+전처리 `e1adeb3`, L2b회랑 `5eae5a8`, 문서 `1f49ec7`. 커밋 시 bash here-string `git commit -m "$(cat <<EOF…)"` 으로 한글 안전.

**그룹배관(번들) 탐지 L4 (2026-06-02, 브랜치 `feat/bundle-detection-l4`)**: 인포그래픽(기하 유사도 그룹 배관 탐지) 기반. **정의=동일 이격간격으로 2회+ 수직/수평 꺾임 공유하는 평행 다발**. **신규 [bundle_detect.py](python_experiments/routing3d_py/bundle_detect.py)** 3단계: ①특징(방향런 압축·Arrow R/H/D·꺾임수·리샘플 방향벡터·extent) ②복합유사도(형태30% Levenshtein+방향30% 코사인+길이20%+규모20%) ③(owner,util) pre-filter→Union-Find(임계0.70)→번들게이트(꺾임≥2 + pitch CV≤0.30)→트렁크z·다발폭·pitch. **신규 [db/schema/route_bundle_group.sql](db/schema/route_bundle_group.sql)**(테이블 + 집계뷰 route_bundle_template). CLI `--project N|--all --write-db|--templates`. **--all=DB 전체 70프로젝트·353그룹·템플릿 275키**. route_db 에 `owner_name`(SOURCE_OWNER_NAME) 추가. pytest +15(260). 개발계획 [docs/routing3d_bundle_detection_plan.md].
- **신규설계 활용(C#, 엔진무변경)**: [Model/BundleStore.cs](csharp/Routing3D.Viewer/Model/BundleStore.cs)(route_bundle_template→(owner,util)·util / route_bundle_group member_guids→guid→group_id) + SceneData.SourceFile + `UseBundlePattern` 토글. **MergeBundleLevels**(유틸 trunk_z→rack_levels) + **BuildBundleCorridorCells**(옵션1=같은유틸 기존배관 전체 넓게 → **옵션2 레인모드**=트렁크고도 ±1셀 수평런만 타이트, route_multi 충돌회피가 인접레인 분산). DbRouteDiag env `R3D_BUNDLE`. **실측(project6 ALL c100)**: 전부OFF 199·rackZ17.3% → 스텁+번들레인 **207·rackZ39.7%**(corridor 28k셀, 옵션1 broad 118k→4배 타이트). **핵심교훈: cell(100)>pitch(~56mm)면 인접레인이 같은 셀로 뭉개져 물리적 패킹 불가 → cell≤pitch/2(25~50mm) 필요**(cell=50 LPS rackZ 92%). route_multi(capi)는 이미 w_corridor>0 시 동적 자기번들링(line 181 add_corridor_cells)+seed corridor.
- **그룹배관 강조 표시(C# 뷰어)**: `ExistingPipe.RoutePathGuid` 추가(로더가 채움) + `BundleStore.GroupIdOf` + **`ShowBundleGroups` 토글** — 번들 멤버를 그룹별 고유색(황금비 색상환 `BundleGroupColor`), 비멤버 흐린회색. member_guids=ROUTE_PATH_GUID 정확매칭. UI 체크박스 '그룹배관 강조'(BundleGroupStatus 라벨).
- **스텁 추출 알고리즘 상세문서**: [docs/routing3d_stub_extraction.{docx,pdf}](docs/routing3d_stub_extraction.docx) 16장(런압축→지터흡수→엘보탐지→점열절단, Python↔C# 1:1). 생성기 `_gen_stub_extraction_algorithm.py`.

**미세격자 탐색량 최적화 (2026-06-02, 브랜치 `perf/fine-grid-hier-corridor`)**: 사용자 "cell≤50 ALL 탐색량 최적화" 요청. **실측 결론: 이미 대부분 해결됨** — 옛 문서의 "10mm ~110s/배관"은 스텁+Implicit+weighted A* 이전 수치. 현재 project6 ALL 208(스텁ON 208/208 매칭): cell=50 **0.14s** · cell=25 **0.5s** · cell=10 **7.7s**. 스텁 라우팅이 랙↔랙 짧은 구간만 탐색해 미세격자를 근본 해소. **추가 구현 = 계층 corridor(coarse 가이드→fine 튜브 하드 제한)**: `astar.hpp` 에 `AllowAll` 술어 + `astar_weighted` 템플릿 `InCorridor in_corridor`(기본 AllowAll → 분기 제거 → **골든 바이트 불변**). capi `coarse_implicit_from_doc`(factor=8 굵은 occ) + `route_hier`(coarse 스냅 가이드 → ±2 튜브 + 종단 연결박스 → fine A* 하드제한, 실패 시 false→폴백). **escalation 게이트**(핵심): route_multi_impl 가 저예산(300k) 직접 A* 먼저 → 성공이면 채택(쉬운 배관 오버헤드 0), 초과(어려운 배관)만 hier. **교훈: 거리 기반 게이트는 역행**(긴 직선까지 hier → cell=50 스텁ON 134ms→24s) → probe 기반이 옳음. 결과: cell≤25 회귀 0·품질 바이트동일, **cell=10 스텁OFF 41s→28s(1.47×)**. ctest 11/11·project1 113/113 불변. 잔여=가중(ε)A*/병렬화. (pybind `routing3d_cpp` 빌드는 기존 결함 `bindings.cpp:134` py::arg 불일치로 실패하나 capi/ctest/뷰어 무관.)

**동적(수렴) 가중 A* + 병렬화 기각 (2026-06-02, 브랜치 `perf/dynamic-weight-astar`)**: 사용자 "가중(ε)A* 추가 튜닝 / 독립 배관 병렬화 진행". **① 동적 가중 A*(채택·기본 ON)**: 정적 `w_heur=2.0` 그리디가 목표/PoC 근처 혼잡·막다른길에서 함정 → 마지막 접근 경로 놓침. `RouteParams.w_heur_near` 추가, `astar.hpp` 가 휴리스틱 가중을 목표거리비로 보간(`w_eff = wn + (wf−wn)·h/h_start`, 먼곳=2.0·근처=1.0=표준 A*). 준최적 상한은 wf. **핵심 게이트: `max_expansions≤0`(무제한)에서만 적용** — 예산-게이트(probe 300k·hier)에서 근처 가중 낮추면 예산초과 escalation 폭주(cell=25 1.3s→33s)라 거대격자 자동 비활성. capi `R3dParams.w_heur_near`+set_params, C# Native/Engine/SceneViewModel(weighted 시 기본 1.0)/DbRouteDiag(env `R3D_WHEUR_NEAR`, 0=끔). **실측 project6 c100: 스텁ON 206→208(완전!)·스텁OFF 199→203, 시간 동일 / cell≤25 무영향 / 골든 불변(w_heur=1→dyn off)**. ctest 10/10. `--selftest`(scene.txt 경로)는 w_heur_near 미직렬화·기본 0 → 194 불변(3자 일치 보존). test_capi.cpp 의 R3dParams positional aggregate init 에 0.0 추가(필드 삽입). **② 독립 배관 병렬화(기각)**: optimistic 병렬(Phase A 마크없이 동시 라우팅→Phase B 순차 커밋·충돌시 재라우팅) 구현, **순차와 바이트 동일**(정확)했으나 c100/c25/c10 전부 wall-clock 이득 0~음수(c10 26.1↔26.8s). 원인=미세격자 A* 가 거대 해시맵 스트리밍하는 **메모리대역 바운드**(스레드 대역 경합) + Phase A 가 마크없는 더 큰 탐색 중복. → 코드 제거. **교훈: 미세격자 가속은 스레드 병렬 아니라 탐색량 축소(스텁·동적가중·hier)가 정답.**

**그룹배관(번들) 정의 v3 재설계 — 세그먼트 동시진행·수평+수직 (2026-06-06, 브랜치 `feat/group-pattern-routing`)**: 사용자 정의 = "동일간격으로 수평·수직 평행 진행 + 진행 중 직교좌표 ±10mm 유지(레인) + 밴딩 각도 ±2° 동일이면 그룹 포함". v2(공간=수평 trunk_axis x/y·centroid 기반)가 **수직 입상 다발을 통째로 놓치고**(사용자 실측 이미지) 분기로 흩어진 수평도 일부 누락 → **세그먼트 co-travel 검출로 전면 재작성**. [bundle_detect.py](python_experiments/routing3d_py/bundle_detect.py): `_extract_runs`(폴리라인→축정렬 직선 런, 우세축+perp 밴드 ±`LANE_TOL_MM`10) → `_axis_bundles`(축 0/1/**2 모두**: sh 공유±10·진행겹침≥`MIN_OVERLAP_MM`300·sp 등간격 `PITCH_GAP_FACTOR`2.5) → 코너 병합(멤버≥2 공유 Union-Find = 같은 배관이 꺾여 이어짐, ±2°는 우세축 전환이 흡수) → `trunk_axis`(총 런길이 최대축) 산출. **꺾임 게이트 폐기**(`MIN_BENDS`=0, 직선 평행도 번들). BundleGroup 에 `trunk_axis` 필드. **SQL**: `ALTER TABLE … trunk_axis`(마이그레이션) + 템플릿 뷰 `trunk_zs` 는 **수평(trunk_axis<2)만**(수직 trunk_z=런중앙z=랙고도 아님 → rack_levels 오염방지) + `n_vert`. **C# 수직 라우팅 활용**: SceneViewModel·DbRouteDiag `BuildBundleCorridorCells` 멤버 인지(memberAware=GroupCount>0·isMember=GroupIdOf≥0) → 번들 멤버의 **수직 입상도 회랑 셀로**(`includeVertical:true`, 트렁크밴드 무관 채택). 라우팅 호출도 includeVertical:true. **실측**: project6 21번들(수직2 포함, UPW_S42·HOT DI_S36·NFW23) · **전체 70프로젝트 1,215번들(수평 x312·y262·수직 z641 — 수직 최다, 이전엔 0)** · 수직키 301. A/B(p6 ALL c100 스텁ON): 번들OFF 208/208·rackZ35.5% → ON(수직레인) 207/208·totalLen↓·**rackZ38.5%**·corridor46,483셀(잔여 1실패는 수직 변경전 수평회랑도 동일, 회랑 트레이드오프=신규회귀 아님). **테스트 17/17**(test_straight_parallel_is_bundle·test_vertical_riser_bundle·test_non_overlapping_not_bundle 추가, 옛 '직선/1엘보 = 번들아님' 2개 새 동작으로 갱신). 빌드 오류0. 문서 [docs/routing3d_bundle_algorithm.{md,docx,pdf}] v3 전면 갱신(생성기 `_gen_bundle_algorithm.py`). **교훈: 평행 다발은 형태가 아니라 공간(같은 축 동시진행+동일간격+레인유지)이고, 진행축은 수평만이 아니라 수직도 대등하게 다뤄야 입상 다발을 놓치지 않는다.** 커밋 시 bash here-string(`@` PowerShell heredoc 실패).

**번들 표시·회랑 멤버십 기준 전환 (2026-06-06, 브랜치 `fix/bundle-display-membership`)**: 사용자 실측(CMP_KSCTA08) — 보라 패턴 표시가 ①1개 배관도 그룹으로 보이고 ②수평 그룹배관(PCMP5615)이 누락. **진단: 데이터(검출)는 정상**(route_bundle_group 전부 ≥2멤버·수평11+수직11), **표시·회랑이 문제**. 원인=`BuildBundleCorridorCells`(표시+라우팅 회랑 공유)가 검출 멤버십이 아니라 '유틸+trunk_z 밴드' 휴리스틱이라 (a)밴드 근처 단독 배관도 칠하고 (b)수평 가지는 등록 trunk_z 밴드에만 칠함. PCMP5615는 6개가 ㄴ자(y런 24264+z런 29015)로 1번들 검출됐는데 z런이 근소히 커 **trunk_axis=z 라벨 → trunk_zs 에서 제외(수직은 랙고도 아님) → 수평부가 표시·경유지 둘 다 누락**. **수정**: `BuildBundleCorridorCells`(SceneViewModel)+`BuildBundleCorridor`(DbRouteDiag)를 **멤버십 기준**으로 — 멤버(`GroupIdOf≥0`)의 **모든 런(수평+수직)**을 trunk_axis·trunk_z 무관 채택, 비멤버 제외(`memberAware && !isMember → continue`), 번들 미적재면 옛 밴드 폴백. 표시=경유지 동일 셀집합. **검증**: PCMP5615 6/6·corridor 8081셀(수평+수직 모두 주입) · project6 ALL 207/208·46483셀 회귀0. **교훈: 패턴 표시·라우팅 활용은 trunk_z 밴드 휴리스틱이 아니라 검출 멤버십으로 구동해야 ㄴ자/축라벨에 무관하게 수평·수직 다발이 모두 경유지가 된다.**

**3-경로방식 + 그룹패턴 톱니제거 + 번들 멤버십·레인 다발 (2026-06-06, PR #21 머지 `07078f8`)**: 사용자 흐름 "그룹패턴 경유가 적용 안 됨 → 적용되니 꾸불꾸불 → 다발이 안 묶임"을 추적해 근본원인 5건 수정 + 3-경로방식 UI 마무리. **① 경로방식 3종 라디오**(최단/그룹패턴 경유=설계코너 강제경유/기존설계 추종=폴리라인복제, `SceneViewModel.RouteMode`+MainWindow). **② 엔진 cell=25 기본값에서 후처리 동작**: `r3d_route_task`가 route_multi 와 달리 **항상 Dense 복셀화**(매 수리 A* 호출마다 1.3억 셀)였음 → 격자>5M 셀이면 **ImplicitOccupancy 자동전환+탐색상한12M**(≤5M Dense 골든 바이트불변, ctest 11/11). 코너/복제 가드 **30M→300M**(cell=25=1.3억 셀 포함). **③ 그룹패턴 톱니 제거(핵심)**: `PolyToCells`가 코너사이를 **한 축순서 L만** 그려 그 L이 막히면 불필요한 A* 톱니(`X5 Y10 X5 Y14…` 계단) 발생 — 실제론 **반대 축순서 L이 자유**였음. `ReplicateCellPath`/`CellPathWithRepair`를 **정점쌍 양 축순서 L 모두 시도**(둘 다 막힐 때만 국소 A*)로 재작성 + 수리 A*는 `StraightenOrtho`(충돌없는 L 직선화)+표준 A*(w_heur=1.0, w_turn 페널티가 최소꺾임 유도). 실측 cell=25 ALKA **avgTurns 33→8.1(≈설계 코너수 7.8)·avgReps 6.1→0.9**. **④ BundleStore MARS 버그(재발!)**: 첫 reader(route_bundle_template)를 `using var`로 메서드끝까지 연 채 같은 conn에 둘째 reader(route_bundle_group) 실행 → Npgsql MARS 미지원 예외 → catch가 삼켜 **GroupCount=0(번들 멤버십 전면 유실)**. `using(){}` 블록스코프로 닫아 **0→22 복구**. **이게 세션초반 "그룹 인식 안 됨" 표시버그의 진짜 근본원인** — membership-driven 표시·회랑·레인이 한 번도 안 켜져 있었음. ([[reference-routing-scene]] 의 "Npgsql 연결당 reader 1개" 함정이 PatternStore(이미 블록스코프 OK)와 달리 BundleStore에서 재발). **⑤ 동일유틸 번들 공유트렁크 레인**: `AssignBundleLanes`(GUI+diag) — 같은 번들 작업을 **distinct 멤버 배관 레인에 1:1 탐욕최근접 배정** → 평행 다발 재현(레인 붕괴 방지). cross-util은 랙레벨로만 공유(번들키 동일유틸 유지, 사용자 선택). 실측 lane 14/14·102/102(전부 distinct). **⑥ 진행다이얼로그 체크박스 라벨색=오버레이색 동일**(복셀#9698A0·점유#DC463C·방문#EBCD3C·경로#3C96F0·패턴#BE78EB). 방문맵은 엔진 A* 탐색흔적으로 유지(최종경로=코너 후처리 교체라 다른게 정상, 사용자 확인). **교훈: ① route_task는 route_multi와 백엔드 정책을 맞춰야 함(Dense-only가 미세격자 후처리를 막음) ② 직교 경로 연결은 양 축순서 L을 모두 시도해야 톱니가 안 생김 ③ Npgsql MARS 함정은 클래스마다 재확인 — 한 connection에서 reader 중첩 금지.** 헤드리스 검증 env: `R3D_GROUPCORNER=on R3D_BUNDLE=on --dbroute 1 25 ALKA|ALL`(지표 lane M/N·avgTurns·avgReps·avgJogs).

**그룹패턴 코너 '말림' 제거 — 자연 엘보 (2026-06-06, PR #22 머지 `2f4e596`)**: 사용자 — 자동설계 꺾임이 기존배관처럼 한 번에 안 꺾이고 안쪽으로 말려 여러번 꺾임(밴딩 불가). 방향런 덤프로 추적: 수평X런(긴)→수직Z라이저(긴) 전환 코너에 **`Y4 X2`(100mm·50mm) 짧은 2축 계단(말림)** — 설계코너가 셀 스냅되며 생긴 staircase + `ReplicateCellPath`가 `StraightenOrtho`를 **수리 구간에만** 적용해 정점쌍 연결 코너의 계단은 직선화 누락. **수정: 조립된 전체 경로에 StraightenOrtho 1회 추가 적용** — 짧은 2축 계단 흡수, **주요 패턴 코너(수평↔수평·수평↔수직=3축전환/방향반전)는 FreeOrthoL(≤2축)이 못 잇어 자동 보존**(그룹 코너 경유 유지). 실측 cell=25 **avgJogs 0.8→0.0(ALKA)·0.1(ALL)·avgTurns 8.1→4.3·성공/레인 불변**(108/110·lane 102/102). **교훈(정정됨, 아래 PR #23 참조): 전체 경로 StraightenOrtho 는 과도 — 패턴 코너까지 단축.**

**그룹패턴 붕괴 회귀 수정 — DeJog (2026-06-06, PR #23 머지 `bdca292`)**: PR #22 의 "조립 전체 경로 StraightenOrtho" 가 **회귀**였다 — 사용자: "그룹패턴 전혀 적용 안 됨, 왜 또 기존문제로 돌아갔나". 원인: StraightenOrtho 는 **임의 2축 자유 L 을 단축**하는데, 내가 "수평↔수평 코너는 3축이라 보존"이라 가정한 게 **틀림**(수평↔수평=2축 전환 → 개방공간에서 FreeOrthoL 이 직선 L 로 단축) → **avgTurns 8.1→4.3**(설계 코너 7.8의 절반 = 패턴 코너 ~절반 소실, 최단처럼 붕괴). ALKA(수직번들=Z라이저=3축)만 일부 살아남아 더 헷갈렸음. **수정: 전체 StraightenOrtho 를 `DeJog` 로 교체** — '같은 축·방향 두 런 사이에 낀 짧은(≤4셀=100mm@cell25) 다른축 런(=말림 계단 한 칸)'만 충돌없는 단일 L 로 흡수, **긴 런 사이 큰 패턴 코너는 불변**. StraightenOrtho 는 국소 킨크용으로 수리 구간에만 유지. 실측 cell=25: ALKA avgTurns 4.3→**7.7(≈코너 7.8 복원)**·avgJogs 0.8→0.4 / ALL 3.9→**8.3(≈7.9)**·108/110·lane 102/102 불변. **교훈: 직교 경로의 '말림(양자화 계단)'은 같은축 사이 짧은 런만 국소 병합해야 한다. 전역 자유-L 단축은 개방공간의 의도된 패턴 코너(2축 수평전환)까지 먹어 패턴 추종을 파괴한다. 직선화 강도는 jog 길이(≤4셀)로 게이트.**

**DDW_AI_DB 완전교체 — 뷰어 (2026-06-06, 진행 중)**: 사용자 "DB를 AUTOROUTINGV7→DDW_AI_DB 완전교체". 결정 5문답=①프로젝트단위=TB_SPACE_GROUP_INFO TAG_GROUP(툴그룹) ②장애물 스코프=그룹 AABB 공간교차 ③작업=TB_ROUTE_PATH 정본(SOURCE_GUID=TB_POCINSTANCES.INSTANCE_ID 조인) ④학습자산=공식 AI 테이블 ⑤완전교체(병행토글 없음). **DDW 스키마 차이**: SOURCE_FILE 없음 · 좌표 MIN/MAX→`AABB_MIN/MAX_*`(+OBB) · POC jsonb→`TB_POCINSTANCES` 테이블 · IS_MAIN→`MAIN_SUB_TYPE`('MainTool'/'SubTool') · `COLLISION_PASS`(0/1 통과플래그) · TB_DUCT_LATERAL→`TB_LATERAL_PIPE`+`TB_DUCT` 분리 · 공식 `TB_ROUTE_DESIGN_GROUP`/`TB_ROUTE_FEATURE_VECTOR`/`TB_ROUTE_SEGMENT_TEMPLATE`/`TB_ROUTE_NODES`/`TB_ROUTE_EDGES`/`TB_AUTO_DESIGN`. **[ObstacleDbLoader.cs](csharp/Routing3D.Viewer/Model/ObstacleDbLoader.cs) 전면 재작성**(DbConfig.Database 기본=`DDW_AI_DB`): ListProjects=TB_SPACE_GROUP_INFO(ProjectId 1..N seq), LoadScene=그룹 AABB±500 공간교차로 TB_BIM_OBSTACLE(COLLISION_PASS→`ObstacleBox.PassThroughOverride`·damper 제외)·TB_EQUIPMENTS(MAIN_SUB_TYPE)·TB_LATERAL_PIPE+TB_DUCT·TB_SPACE_INFO·LoadRoutesAndTasks(TB_ROUTE_SEGMENT_DETAIL⨝SEGMENTS⨝ROUTE_PATH, 작업+기존배관 폴리라인). **[BundleStore.cs] 이관**=TB_ROUTE_DESIGN_GROUP(GROUP_ID·MEMBER_ROUTE_GUIDS text[]→guid맵). **격자 폭발 버그·수정(핵심)**: 거대 공유 슬래브(건물 전체 X 0~443m·Z 48m)가 공간필터로 새어 4433×1330×487=28억 셀·313s. **ComputeGrid 를 그룹 AABB 박스 3축 전부로 클램프** → 136×149×91≈184만 셀·139s. **검증** `--dbroute 1 100 ALL R3D_STUB=on R3D_BUNDLE=on`: 장애물1177(통과528)·장비1·덕트20·작업151·스텁151/151·**번들149키**·success 146/151(잔여5=혼잡/막힘 CBS). **남은 일**: PatternStore→공식 TB_ROUTE_SEGMENT_TEMPLATE(현재 null 폴백·무해, 스텁라우팅은 StubExtractor 직접) · Python 레퍼런스(obstacle_db/scene/route_db/pattern_learn/bundle_detect) 재작성(후순위). **교훈: 공간교차 스코프만으론 부족 — 격자 origin/shape 를 선택 그룹 AABB 로 직접 클램프해야 건물전체 슬래브가 셀 폭발을 일으키지 않는다.** 분석문서 docs/routing3d_ddw_ai_db_migration_analysis.md.

**DDW_AI_DB 완전교체 — 전 계층 완료·머지 (2026-06-06, PR #24 머지 `404bc32`)**: 위 '진행 중' 항목 완결. A→B→C→D 순차. **A Python 레퍼런스 재작성**(obstacle_db=xy_bbox 스코프·AABB·COLLISION_PASS·damper, route_db=GroupInfo·TB_EQUIPMENTS·TB_LATERAL_PIPE+TB_DUCT·TB_ROUTE_PATH owner=EQUIPMENT_NAME, scene=작업이 route_path, pattern_learn·bundle_detect 그룹 기반). DB 테스트 DDW 현실로 갱신(project1=WTNHJ02·장애물1177·작업151). **pytest 260 passed·2 skipped**(skip=route_stub_pattern 빈 저장소 — pgvector 스키마 DDW에 재적용함). route_db/scene CLI·학습 파이프라인(스텁287·번들22) DDW 검증. **B PatternStore→공식 TB_ROUTE_SEGMENT_TEMPLATE**(20k행 사전적재, 우리 route_stub_template 적재 불필요): SEGMENT_ROLE A_EQUIP_STUB→EQUIP(face=START_DIR_UNIT 우세축)·C_DUCT_ENTRY→DUCT(face=−END_DIR_UNIT 우세축), B_BRIDGE 무시. 실측 EQUIP=−z·DUCT=+z 도메인규칙 일치, **129키 로드**(이전 "없음"). L3b ANN은 공식테이블에 anchor AABB 없어 비활성(TryGetFaceAnn 항상 false·AnnKeyCount=0, 과거 LOO가 1키만 채택한 한계기능이라 회귀無). **C 배관자재 cube box**(Msg12): BuildModel 이 기존배관 내부정점(세그먼트 조인트, 양끝 PoC 제외)마다 은회색 cube(관경×1.4)+범례, 빌드 컴파일·코드검증(GUI 시각확인은 데스크톱). **D**: 2커밋(PDF 날짜리네임+md→pdf변환기 / DDW+UX 코드+문서) → PR #24 생성·머지·브랜치삭제. **교훈: 공식 학습테이블이 우리 커스텀 저장소를 대체할 수 있으면(TB_ROUTE_SEGMENT_TEMPLATE) 적재단계가 사라진다 — face는 stub role+dir_unit 우세축으로 재구성, EQUIP은 이탈방향·DUCT는 진입반대(표면법선).** [[reference-obstacle-db]]/[[reference-routing-scene]] 의 AUTOROUTINGV7 스키마는 폐기됨(이 메모리·repo CLAUDE.md §8 가 DDW 정답).

**DDW 뷰어 표시 보강 + 전수 분석 (2026-06-06, PR #25~#31, main 머지)**: DDW 완전교체 후 사용자 피드백 반영 연속 작업. **#25/#27 공간영역**: `TB_SPACE_INFO` 층(건물 전체 "Dinno Building Area" 443m×133m×25m)이 작업영역(그룹박스 12.5m)을 압도 → 그룹 AABB 3축 클리핑 + **와이어프레임**(처음 cube box 로 했다가 사용자 정정 → 와이어). **#26 장애물**: 바닥/천장 슬래브도 그룹 AABB 로 클리핑(`ObstacleDbLoader` 장애물 루프). **라우팅 불변**(엔진이 이미 장애물∩격자만 복셀화 → 미리 잘라도 동일, 146/151·totalLen 798300 바이트동일, 1177→1139=작업 Z 밖 슬래브 38개 제외). **#28 배관 자재 실제화**: 흰 cube 가 폴리라인 내부정점(휴리스틱)이라 실제 자재와 달랐음 → `TB_ROUTE_SEGMENT_DETAIL.TYPE` 의 실제 부속(PIPE/POC/BENDING 제외 = ELBOW/TEE/VALVE/FLANGE/REDUCER/UNION/GLAND/GASKET/FILTER/TAKEOFF…)을 FROM/TO 중점에 타입별 색 cube + 독립 토글(`ShowFittings`·UI '배관자재'). 모델 `SceneData.PipeFitting`·로더 `LoadFittings`. 실측 group1: ELBOW 484·TEE 364·FLANGE 164…≈1198. **#29/#30 클릭 정보 확장**: `SelectObjectAt` 에 기존배관(폴리라인=중심선 거리 `DistPointToSeg`, 큰 장애물보다 우선·부속이 더 구체적이라 양보)·배관자재(작은 박스)·PoC 마커(시작=장비/종단=덕트, 구 반경 박스) 추가 → `Describe{Pipe,Fitting,Poc}` 모달리스 표시. 좌측 그룹/유틸 필터 동일 적용. 이제 클릭 가능: 장애물·장비·덕트·기존배관·배관자재·PoC. **#31 전수 분석 엑셀**: `out/_gen_route_analysis_xlsx.py`(openpyxl) → `out/ddw_route_analysis.xlsx`(전체 7,052 경로 시트 + 유틸그룹×종단소유자종류 교차표). `TB_ROUTE_PATH`+`TB_POCINSTANCES` 조인. **종단 PoC 데이터 진단(데이터/로직 오류 아님)**: Exhaust 종단명이 'Damper-...-Duct' 로 보이는 건 정상 — 종단명=`TARGET_OWNER_NAME`, 위치=`TARGET_POS`, 실체=`TARGET_GUID`→`TB_POCINSTANCES`(UTILITY=ACID). 배기 배관이 덕트에 직접 안 붙고 **덕트 부속(댐퍼/엘보/테이크오프)에 접속**하는 설계. 댐퍼 소유자 `OWNER_INSTANCE_TYPE='MODEL'`(TB_DUCT/OBSTACLE 에 없어 화면엔 PoC 만). 분포 DUCT 669·Damper 122·PIPE 88·…. **교훈: 클리핑은 표시 정합성만 — 엔진이 격자로 교차하므로 라우팅 불변. '실제 부속'은 TB_ROUTE_SEGMENT_DETAIL.TYPE(PIPE/POC/BENDING 제외)이 정답. 종단 owner 는 덕트 본체가 아니라 덕트 부속일 수 있음(정상).** 커밋 메시지에 작은따옴표 있으면 bash here-string 깨짐 → `git commit -F 파일`.

**Why:** development_plan.md / phase1_plan.md 마스터 계획. 사용자가 단계별 순차 진행 요청.

**How to apply:** 구현은 docs/phase1_plan.md 를 따른다. Phase 3 C++ 1:1 포팅이 쉽도록 자료구조 단순·명시적 유지. 코드 문서화는 [[feedback-code-docs]] 규칙 적용.
